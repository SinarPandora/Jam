plugins {
    id "idea"
    id "java"
    id "scala"
    id "distribution"
    id "application"
}

sourceCompatibility = "11"

scala {
    zincVersion = "1.3.5"
}

application {
    mainClass = 'o.lartifa.jam.Bootloader'
    applicationDefaultJvmArgs = ["-Xmx256m", "-Xms64m", "-ea", "-server"]
}

dependencies {
    implementation(project(":jam_self"))

}
